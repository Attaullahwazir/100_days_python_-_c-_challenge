width = float(input("Enter the width of the rectangle: "))
height = float(input("Enter the height of the rectangle: "))

area = width * height
perimeter = 2 * (width + height)
print(f"the width {width} and height {height} of area in rectangle is {area}")
print(f"the width {width} and height {height} of perimeter in rectangle is {perimeter}")